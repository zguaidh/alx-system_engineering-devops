CREATE DATABASE tyrell_corp;
USE tyrell_corp;
CREATE TABLE nexus6 (
    -> Id int,
    -> Name varchar(255)
    -> );
GRANT SELECT ON tyrell_corp.nexus6 TO 'holberton_user'@'localhost';
INSERT INTO nexus6 (Id, Name) VALUES (1, "Biba");
